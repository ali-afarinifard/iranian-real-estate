# API Configuration
# In development, MSW intercepts all requests automatically.
# In production, point this to your real backend.
NEXT_PUBLIC_API_URL=/api

# Set to "true" in development to activate MSW mock layer
# Set to "false" in production when connected to a real backend
NEXT_PUBLIC_USE_MSW=true
